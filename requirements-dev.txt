# Dependencias de desarrollo/test. La Lambda en sí no tiene
# dependencias de terceros aparte de boto3, que ya viene incluido en
# el runtime de Lambda, así que no hace falta empaquetarlo.
pytest>=7.4,<9
pytest-cov>=7.1.0,<8
moto[s3]>=5.2.3,<6
boto3>=1.43.73
ruff>=0.6,<1
